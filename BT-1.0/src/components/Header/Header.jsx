import {APPLICATION_TITLE} from '/common/constants.js'
export default function Header(){
    return (
        <div className='initial-message-content'>
             <p>{`${APPLICATION_TITLE}`}</p>
    </div>
    );

}